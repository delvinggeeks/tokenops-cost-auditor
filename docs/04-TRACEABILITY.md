# Traceability Matrix — TokenOps Cost Auditor v1.0

Req → HLD component → LLD module(s) → Test ID(s) → public docs page. Test
details in 05-TEST-PLAN.md. CI gate: every M-priority FR/NFR row must have ≥1
passing test; matrix checked in review before merge to main. DOC column added
at D-DOCS (DOCS-PLAN §5.4): the docs-site page that covers the requirement
publicly ("—" = internal-only, no public page needed).

**Scope of this matrix (settled convention, so gates don't re-derive it).** This
matrix tracks CUSTOMER-FACING product requirements (FR/NFR in 01-REQUIREMENTS.md)
only. Loop-engineering / SDLC / CI tooling — the LE-* cards, the gate agents, the
gate-round harness (`scripts/gate_round.py`), `scripts/check_authorship.py`,
`scripts/pricing_verify.py` and peers — is governed by CLAUDE.md rule 7 (TOKEN
ECONOMY, TE-1..11) and docs/09-SDLC, NOT by 01-REQUIREMENTS, so it owns no row
here and CLAUDE.md rule 5's "implemented requirement" trigger does not fire for it.
Relatedly, X-04's ban on "LLM-generated narrative in reports" scopes to the
customer-facing audit report (FR-16); it does NOT govern internal engineering
surfaces such as the gate-round PR comment.

| Req    | HLD | LLD module(s)                              | Tests | DOC (docs-site/) |
|--------|-----|--------------------------------------------|-------|------------------|
| FR-01  | C1,C2 | web/upload, api/routes_upload, ingest/*  | T-ING-01..04, T-API-01 | quickstart, api/overview |
| FR-02  | C2  | ingest/normalizer                          | T-ING-05..07 | quickstart |
| FR-03  | C2  | ingest/validator                           | T-ING-08..09 | concepts/how-it-works |
| FR-04  | C2  | cli.py                                     | T-CLI-01 | quickstart |
| FR-05  | C3  | pricing/table, data/prices.yaml            | T-PRC-01..03 | concepts/pricing-data |
| FR-06  | C3  | pricing/coster                             | T-PRC-04..05 | concepts/pricing-data |
| FR-07  | C4  | rules/d1_oversized_model                   | T-RUL-D1-01..03 | concepts/waste-classes/oversized-model |
| FR-08  | C4  | rules/d2_missing_cache                     | T-RUL-D2-01..03 | concepts/waste-classes/missing-cache |
| FR-09  | C4  | rules/d3_prompt_bloat                      | T-RUL-D3-01..02 | concepts/waste-classes/prompt-bloat |
| FR-10  | C4  | rules/d4_retry_storm                       | T-RUL-D4-01..02 | concepts/waste-classes/retry-storms |
| FR-11  | C4  | rules/d5_unbounded_max_tokens              | T-RUL-D5-01..02 | concepts/waste-classes/unbounded-max-tokens |
| FR-12  | C4  | rules/d6_chatty_loop                       | T-RUL-D6-01..03 | concepts/waste-classes/chatty-loops |
| FR-13  | C4  | rules/findings, rules/registry             | T-RUL-00, T-RUL-EV-01 | report/reading-a-report |
| O-4 workspace settings home (R-ORG, ROADMAP §3 #7; founder "settings home good, wire it, fold Developer in" 2026-07-26) | C6,web,persistence | app/_settings_nav.html — the settings-home TAB SPINE (General/Members/Developer/Sign-in/Audit-log), server-rendered anchors (deep-linkable, no JS), active tab via aria-current from each route's `settings_tab`; wa-design.css .settings-tabs (design source synced — CSS-parity law). The spine is included on settings.html/members.html/developer.html; NEW settings_signin.html (email magic-link + web.routes_auth.enabled_federations — no dead SSO control, enterprise SSO stated as future) and settings_audit_log.html (repo.list_workspace_audit_log — the append-only AuditLogEntry governance trail scoped to the active workspace's member emails since the log has no workspace_id; FR-22 counts/metadata only; honest empty state). NEW GET /settings/sign-in + /settings/audit-log (routes_settings). Sidebar Account group collapsed to Settings + Billing — Members/Developer are now tabs, the single Settings item stays lit across all. ORGANIZING surface, no new capability; O-2 RBAC governs Members within the tab unchanged. Engine untouched | tests/test_settings_home.py (every tab renders + carries the full spine; each marks its own tab active; sidebar collapsed to one Settings; sign-in shows the account + future-SSO; audit log surfaces workspace actions + honest empty state) + test_developer_platform reachability updated (sidebar→Settings→Developer tab) | — |
| FR-14  | C5  | report/model, render_json, render_pdf      | T-REP-01..04 | report/reading-a-report |
| FR-15  | C5  | report/signer, web/report                  | T-REP-05..06 | api/overview |
| FR-16  | C5  | report (synthetic fixture)                 | T-REP-07 (S) | report/worked-example |
| FR-17  | C1  | web/auth (magic link)                      | T-AUTH-01..04 | api/overview |
| FR-18  | C7  | payments/*, api/routes_webhooks            | T-PAY-01..05 | api/overview |
| FR-19  | C1  | web/admin                                  | T-ADM-01..05 | — |
| FR-20  | C8  | mail/*                                     | T-MAIL-01 (S) | — |
| FR-21  | C6  | lifecycle/purge, lifecycle/auditlog        | T-LIF-01..03 | concepts/data-handling |
| FR-22  | C4,C6 | rules/findings(EvidenceRef), persistence; extended to connector/streamed tiers (founder 2026-07-20, GRAND ORDER v2 — v1.5 connectors must add tier tests) | T-LIF-04, T-RUL-EV-01 | concepts/data-handling |
| FR-23  | C1  | web/templates/landing                      | T-WEB-01 | index, legal/privacy |
| FR-24  | C2  | scripts/exporters/claude_code_export.py    | T-EXP-01..02 | quickstart |
| FR-25  | C1  | main.py router mounting, api/*             | T-API-03 | api/overview |
| FR-26  | C2  | api/routes_upload, persistence (idem keys) | T-API-04..05 | api/overview |
| FR-27  | C7  | api/routes_webhooks, persistence (events)  | T-PAY-06..07 | api/overview |
| FR-28  | C5  | report/model, render_json, render_pdf      | T-REP-08 | concepts/pricing-data |
| FR-29  | Ops | scripts/pricing_refresh.py                 | T-OPS-04 | concepts/pricing-data |
| FR-30  | C5  | report/model (equiv-spend flag), _report_body | T-REP-09 | quickstart |
| FR-31  | C1,C6 | SHIPPED at WP-PIPELINE-UI: web/routes_runs + app/runs.html (full runs ledger, purged rows metadata-only, FR-31 count line); migration 017 stage_events/pull_events/alert_checks; runner + connectors/source_audit stage recording (honest zeros incl. every detector); connectors/pull PullEvent success + record_pull_failure; alerts/dispatch AlertCheck rows | tests/test_runs.py (stage order both pipelines, FR-19 replace, pull ledger user-safe failures, alert silence rows, /runs journey: kit ribbon, purged listing, pre-history honesty, in-flight poll) | — |
| V15 R-Q1 | C4,C5 | rules/aggregate (d1/d2/d3 aggregate estimators; INACTIVE law), connectors/source_audit (tier+coverage) | T-AGG-01..05, T-SA-01..03, T-REP-03 | engineering/performance (tier note pending WP-7) |
| V15 R-CONNECT | C6 | connectors/{openai,anthropic}_usage, pull (idempotent upsert + stats), schedule (tick), crypto (HKDF/Fernet) | T-CON-01..06, T-SCH-01..03, T-KEY-01..03, T-V15-MIG-01 | — |
| V15 R-Q5/Q6 | C6 | web/routes_sources (plan gating, revoke deletes ciphertext) | tests/test_sources_routes.py | — |
| V15 WP-3b alerts | C6 | services/alerts (observe-only), web/routes_alerts (threshold editor — the single editor, linked from Settings) | T-ALR-01..05, tests/test_alerts.py::TestAlertsPage | — |
| V15 WP-4 statement | C5,C6 | services/statements/build (R-Q9 + R-STMT-MONTH), web/routes_statements | T-STMT-01..03 | — |
| V15 WP-5 settings | C6 | web/routes_settings, services/lifecycle/purge (one purge_one primitive: scheduled/admin/customer) | T-SET-01..03 + purge scoping/idempotence | — |
| R-PRICING-FINAL-2 | C6,C7 | config (list+launch prices, cohort, gates), payments/plans (cohort_used/launch_open/viewer_currency — the code-enforced flip), landing/billing/alerts templates (one currency per view), scripts/daily_digest (cohort-full founder notice) | tests/test_pricing_final.py | — |
| R-DAILY-LOOP | C6,C8 | connectors/daily (digest + 50/80/100 budget stages, audit-identical rate math), schedule.tick, dashboard/metrics yesterday_spend + widget, migration 008 (users.last_daily_digest_at) | tests/test_daily_loop.py | — |
| R-FED-MAJORS | C6 | web/routes_auth FEDERATIONS registry (Google/Microsoft/GitHub, cookie-pinned state, full-pair gating) | tests/test_federation.py | — |
| R-LIVE-PRICING | C3,Ops | pricing/table (overlay merge, append-only), scripts/pricing_sync (fetch LiteLLM feed → gate-validate → auto-write overlay; --cover-from-usage self-heals + re-audits), ofelia (pricing-sync-refresh daily, pricing-cover 3h), scripts/daily_digest (sync FYI + held-swing alert) | tests/test_pricing_sync.py | concepts/pricing-data |
| R-LIVE-AUDIT | C6 | web/routes_sources (_kickoff_first_pull creates queued Audit synchronously, drives queued→processing→done/failed), connectors/source_audit (finalize a pre-created row), templates/_wizard_verdict (land on the live theater) | tests/test_source_audit.py::TestLiveAuditLifecycle | — |
| R-FLYWHEEL L3 (deterministic-now) | C6,C8 | services/forecast (before-the-invoice month-end projection + overspend anomaly, alert-only/X-02, Honesty-Law basis, holds the alert on an unpriced baseline), dashboard/metrics.forecast + _forecast.html widget, connectors/daily (digest heads-up line + subject), config (connect_backfill_days 365, forecast_overspend_pct) | tests/test_forecast.py, tests/test_daily_loop.py::TestForecastAnomalyInDigest | — |
| FR-32 | C5,C6 | services/dashboard/explorer (filter/compose + overlap law + serialize_filters), web/routes_explorer (+saved-view CRUD), templates/app/explore.html, help_registry (explore destination), migration 014 saved_views (C3, R-PROCEED — export HELD on the data-export trigger) | tests/test_explorer.py — T-EXP-F-01..09 map in module docstring + TestBareAudits + TestSavedViews (whitelist sanitization, scoping, replace-on-name, stated limit) | — |
| R-MULTI-SOURCE | C6 | migration 013 (audits.source_id + sources.key_fingerprint, additive), connectors/crypto.credential_fingerprint, connectors/pull (backfill), connectors/source_audit + routes_sources (attribution stamp; fingerprint dedup replaces per-provider block; label suffixing), explorer per-account filter, sources.html View-usage links | tests/test_connect_wizard.py::TestMultiSource, tests/test_connectors.py::TestFingerprintBackfill, tests/test_explorer.py::TestPerSource | — |
| R-F1-SIGNOFF | C1,C5,C6 | promise copy: report/model.DATA_HANDLING, _public_shell footer, legal/{terms,privacy}.html, docs-site mirrors; users.benchmark_sharing + migration 015; routes_settings save_benchmark_pref (audit-logged) + settings.html surface | tests/test_settings.py::TestBenchmarkSharing, updated FR-23 consts in tests/test_{polish,docs_site,auth}.py, tests/test_report_web.py | legal/privacy |
| M-FLY-0 (docs/12 Stage 3) | C6,Ops | services/flywheel/{frame,cohort} (FRAME_COLUMNS allowlist + ENUM_OR_ID law, cohort_pseudonym HKDF, rung thresholds from config), scripts/flywheel_extract.py, scripts/daily_digest (rung line), routes_admin (row), config flywheel_* | tests/test_flywheel.py (12 tests — determinism incl. cross-run tie-break, schema law, pseudonymity, R-F1 exclusion, boundaries 9/10 + 24/25 + config-mutation guard, founder surfaces rendered, package posture) | — |
| M-FLY-1 (docs/12 L1) | C5,C6 | services/flywheel/benchmarks (nearest-rank-inclusive over engine savings_pct — NOTES golden FOUNDER-VERIFIED; REPORT_BLOCK_KEYS leakage allowlist), dashboard/metrics.benchmark + widgets/_benchmark.html (mockup ux-gated), report model.benchmark optional field + render_json conditional + _report_body block, runner/source_audit attach | tests/test_flywheel.py::TestBenchmarks (goldens, dormancy-absent, leakage, opt-out shrinks pool, report-block exhaustive keys, fixture reports byte-identical) | — |
| WP-CC-LINK (docs/12 T3) | C1,C2,C6 | migration 016 (link_codes + devices; consent_at NOT NULL by schema), web/routes_devices (consent-refusing link, hashed tokens, ship via T1 pipeline + FR-26, revoke), cli link/ship (stdlib HTTP, TTY-only consent, --cron helper), services/collector/transcripts (exporter core lifted; script now a wrapper), sources.html machines surface + _link_code partial | tests/test_collector.py (consent floor both halves, full journey, one-shot/expiry codes, idempotent re-ship, lapse pause, revoke) + tests/test_exporter.py unchanged | quickstart |
| WP-CLOUD-T2 C-A (PLAN-TAAS §2) | C1,C2,C6 | connectors/azure_usage (Azure Monitor metrics adapter, Entra client-credentials, four-field JSON credential, cached_tokens=0 by construction), pull/validate registries, rules/aggregate CACHE_BLIND gating (d2 never runs on fabricated zeros; coverage says "not observable on Azure"), wizard azure branch (cred-grid, portal steps, RBAC trust copy), get-logs tab + sources buttons + account-paths guide updated, prices.yaml azure-openai Global-Standard rows + goldens G16-G19 (FOUNDER-VERIFY before deploy, NOTES derivation) | tests/test_azure_connector.py (credential pack/refuse, metrics merge FR-22, registry pull, Azure-worded verdicts, cache-blind honesty, wizard journey incl. fingerprint dedup) | — |
| WP-CLOUD-T2 C-B (PLAN-TAAS §2) | C1,C2,C6 | connectors/bedrock_usage (CloudWatch AWS/Bedrock metrics via stdlib SigV4, three-field JSON credential, ModelId normalization, anthropic token composition incl. cache reads/writes — d2 RUNS here, no cache-blind gating), pull/validate registries + AWS-worded verdicts, wizard registry-driven fields refactor + honesty_note in main column (ux C-B note 4), get-logs tab + account-paths guide updated, prices.yaml bedrock rows mirror anthropic (on-demand parity; Nova/Llama/Mistral unpriced per FR-28) + goldens G20-G23 (FOUNDER-VERIFY before deploy) | tests/test_bedrock_connector.py (credential/region refusal, normalization, SigV4 determinism + secret hygiene, composition, typed refusals, registry pull, d2-active pin, wizard journey) | — |
| R-AUTO-PRICING (founder 2026-07-23) | C3,Ops | scripts/pricing_verify.py (strict agent gate: every current rate row corroborated exactly per-1M by an independent machine source or the release fails; --stamp maintains last_verified = last successful agent verification), pricing/table.entries() public accessor, CI step (ci.yml) + runbook §2 step 4b, "human-verified" copy replaced by "machine-verified against independent published price data" across report model/template, help_registry, docs-site, PRD | tests/test_pricing_verify.py (unit conversion, azure/ + bedrock dated-key mapping, full pass over the real table, mismatch NAMED + exit 1, uncovered exit 1, green exit 0, structural cache_write default not compared) | concepts/pricing-data |
| WP-DEVOPS-OBS (founder "proceed" 2026-07-23) | C9,Ops | obs/errors (Sentry env-gated activation: optional `obs` extra, FR-22 before_send scrubber, release=deployed tag), config.release_tag + provision.sh RELEASE_TAG upsert, .github/workflows/deploy.yml (workflow_dispatch=approval ritual; gate job re-runs chain+strict verify on the tag; backup→provision→smoke→auto-rollback to prior RELEASE_TAG), .github/PULL_REQUEST_TEMPLATE.md (REV-X + laws), runbook §10 lifecycle + log-rotation audit + activation checklist | tests/test_obs_errors.py (scrubber strips payloads/headers/cookies/breadcrumbs/user; stack+url survive; tolerates minimal events) | engineering/integration |
| S-0 (R-SDK-PLATFORM, PLAN-SDK) | C1,C2,C6 | web/routes_ingest (POST /api/v1/ingest — strict-allowlist FR-22 door with offenders NAMED, bounded scalars, FR-26 idempotency, 60/min limit, paid_via="sdk" + key attribution; mint shown-once + revoke-deletes-hash, Pro+ both halves), models.IngestKey + migration 018, _ingest_key.html reveal (key-truth grammar, full DSN), sources.html SDK & API card, runs trigger "SDK ingest", explorer "(SDK)" source options; records land as the documented generic-CSV contract → zero new parser code | tests/test_ingest_api.py (journey mint→POST→audit→/runs→/explore, envelope-aware FR-22 refusals, idempotent replay, revoke stops ingest, lapse pauses honestly, free-plan gate) | api/overview |
| S-1 (R-SDK-PLATFORM, PLAN-SDK) | C1,C2 | sdk/ package: extract (the FR-22 boundary — reads only usage/model/timing, never choices/content; anthropic cache composition), instrument (openai + anthropic auto-patch; wrapper runs the REAL call first + returns it unmodified, records best-effort in try/except — X-01 observe-only; idempotent, version-tolerant), batcher (bounded queue drops-never-blocks, background flush, stdlib urllib transport, FR-26 idempotency key, swallows network failure, atexit flush), init/record/flush/close + DSN parse; _ingest_key.html Python snippet; quickstart SDK section | tests/test_sdk.py (FR-22 by construction — text-carrying response yields text-free record; observe-only — broken recorder never breaks the call; anthropic composition; bounded batcher; DSN parse; end-to-end SDK-built records → /api/v1/ingest → done audit; installers no-op when lib absent) | quickstart |
| WP-CLOUD-T2 C-C (PLAN-TAAS §2) | C1,C2,C6 | connectors/vertex_usage (Cloud Monitoring token_count metric via SA-JSON credential; RS256 JWT→OAuth token exchange with stdlib+cryptography, no new dep; input/output split → prompt/completion, cached 0 by construction; model normalization), pull/validate registries + Google-worded verdicts, rules/aggregate vertex-ai in CACHE_BLIND_PROVIDERS (d2 not run; coverage "not observable on Vertex", provider-named), wizard textarea field (registry-driven) + SA-JSON validate branch, get-logs tab + account-paths guide updated, prices.yaml vertex-ai Gemini rows + goldens G24-G27 (AGENT-VERIFIED 35/35 via pricing_verify — R-AUTO-PRICING, no human gate) | tests/test_vertex_connector.py (SA-JSON parse/refuse, normalization, RS256 sign path, input/output split counts-only, typed verdicts, registry pull, d2 cache-blind, wizard textarea journey incl. SA-json dedup) | — |
| Works-with rider (R-SDK-PLATFORM §5) | C1 | landing.html four-tier coverage map (connect-card grammar reused, no new CSS); hero-sub + og:description + connect-tools de-staled after S-1 (the "No SDK" claim was false once the SDK shipped) — honest seat-tool truth (Copilot/Cursor/Lovable/Figma) never overclaimed | tests/test_landing_budgets.py::TestWorksWithRider (five providers named, SDK/API/upload paths present, seat tools honest, "No SDK" gone) | index |
| WP-COPILOT-AGG (R-COPILOT, PLAN-COPILOT) | C2,C5 | services/copilot/seats (parse GitHub seats JSON/CSV — DATES ONLY, logins never parsed; idle/never-used/pending analysis; d7_idle_seats finding, rate as a STATED input not a verified figure — R-COPILOT P1), services/copilot/report (seat finding in the existing report shell, tier="seat-governance", equiv_spend framing), routes_copilot (POST /copilot/seats upload → signed report; bounded knobs), get-logs Copilot tab with a real upload form (R-VERTICAL click path) | tests/test_copilot_seats.py (FR-22 login-never-parsed even when present, CSV+JSON parse, idle analysis, finding-labeled-seat-governance + rate-is-input, upload→report journey, form reachable, malformed 400) | quickstart |
| API reference docs (founder 2026-07-24) | C1 | docs-site/api/reference.md — curated Anthropic-style reference: ingest DSN auth (Bearer ik_), POST /api/v1/ingest with the counts-only schema + multi-language examples (curl/Python/requests+SDK/TypeScript/Go tabs), audit status, signed report retrieval, the Python SDK, idempotency, rate limits, NFR-14 error envelope + code table, webhooks; mkdocs nav row; endpoints.md (auto-generated) left untouched | tests/test_docs_site.py::TestApiReferenceAccuracy (real ingest path/auth, error codes tied to ERROR_CODES, language tabs present, counts-only not overclaimed, in nav) | api/reference |
| S-6 (R-SDK-PLATFORM, PLAN-SDK — READ platform, founder "full S-6 incl OAuth apps" 2026-07-24) | C1,C2,C6 | web/api_scopes (READ_SCOPES — read:audits/read:findings, NO write scope), web/api_auth (unified rt_/at_ bearer resolver → ReadPrincipal, tenant-scoped at the web boundary, scope→403, app-revoke kills tokens), web/routes_api_read (GET /api/v1/audits + /audits/{id}/findings — counts/dollars only FR-22, tenant-isolated, other-tenant=404 not 403), web/routes_oauth (OAuth 2.0 authorization-code + PKCE S256 server: /oauth/authorize consent bound to owner via signed blob = CSRF-safe, byte-exact redirect_uri match / no open-redirect, /oauth/token single-use codes + client_secret + PKCE, RFC-6749 errors), web/routes_developer (Developer Settings: mint/revoke read tokens, register/revoke OAuth apps, Pro+ gated, shown-once reveals, hash-only at rest), models.ApiToken/OAuthApp/OAuthAuthCode/OAuthAccessToken + migration 019, developer.html + _api_token.html + _oauth_secret.html + oauth/authorize.html + oauth/error.html, sidebar Developer nav (reachability), 403 in ERROR_CODES; reference.md read+OAuth sections | tests/test_developer_platform.py (scopes unit; token journey mint→read→scope-403→revoke→401, tenant isolation + counts-only; OAuth register→authorize→exchange→read, single-use codes, bad PKCE, wrong secret, unregistered redirect, deny, owner-binding CSRF, app-revoke kills token, logged-out interstitial), tests/test_docs_site.py::TestApiReferenceAccuracy (read routes registered, scopes match code, OAuth+403 documented) | api/reference |
| O-0 (R-ORG, PLAN-ORG — workspace spine, founder "proceed with O-0" 2026-07-24) | C6,persistence | models.Workspace + models.WorkspaceMember (tenancy root; owner membership), workspace_id on the 10 owned tables (audits/sources/ingest_keys/api_tokens/oauth_apps/saved_views/alert_rules/subscriptions/statements/devices), migration 020 (creates the 2 tables + backfills a workspace-of-one per user + stamps workspace_id 1:1), repo.get_or_create_workspace/workspace_id_for + get_or_create_user auto-creates the workspace-of-one + create_audit stamps it + 10 creation sites stamp workspace_id, routes_settings workspace section + POST /settings/workspace/rename (owner rename), settings.html Workspace surface. Reads STAY user_id-scoped in O-0 (correct @ 1:1; re-scope in O-1). Engine untouched — services/rules + services/pricing never learn workspaces (R-ORG) | tests/test_workspace_spine.py (workspace-of-one on new user, idempotent, write-path stamps audit + ingest_key, ISOLATION — 2 workspaces distinct + B invisible to a query scoped to A, rename journey owner-only, empty-name 400, engine-tenant-blind grep guard, ingest journey regression) | (O-0 has no doc page yet; PLAN-ORG) |
| O-1b backend foundation (R-ORG, PLAN-ORG — members backend, founder "go with the recommendations" 2026-07-24) | C6,persistence,web | migration 021 (users.active_workspace_id backfilled to personal; workspace_invites — email + role + one-shot hashed code, LinkCode grammar; workspace_id on alert_events+alert_checks backfilled; payments EXCLUDED — billing visibility is O-2); repo.active_workspace_id made SWITCHABLE (returns User.active_workspace_id iff a live membership, else personal — leak-safe: always a workspace the caller belongs to, never None) + repo.set_active_workspace (validates membership) + repo.list_memberships; billing re-scope (founder Q3 ruling: one sub per workspace, members inherit) — entitlements/entitlements_for/viewer_currency read the active workspace's sub, entitlements_from_workspace + entitlements_for_workspaces added, scheduler keys on the source's workspace, apply_event stays payer-keyed; member-visibility — AlertEvent/AlertCheck display reads (activity feed, /runs ledger, alerts history) flip to workspace + writes stamp workspace_id, per-recipient dedup + Payment stay user-scoped. BEHAVIOR-PRESERVING under 1:1; the member-facing vertical (invite/accept/switch/revoke UI + journey) completes O-1b. Engine untouched | tests/test_workspace_spine.py (existing green under the switchable resolver; O-1b member journey tests land with the UI vertical) + migration 021 rehearsed (upgrade+backfill+downgrade) + affected suites green | (PLAN-ORG O-1) |
| O-1 read re-scope (R-ORG, PLAN-ORG — the sweep, founder "proceed with O-1" 2026-07-24) | C6,persistence,web | repo.active_workspace_id (the single READ-scope chokepoint; O-1a returns the personal workspace so the flip is behavior-preserving @ 1:1; HARDENED never to return None for a real user → a read can never scope `workspace_id IS NULL` and match every tenant's un-stamped rows). DISPLAY reads flipped from user_id→workspace_id across services/dashboard/{metrics,savings,explorer,activity}, services/{forecast,statements/build}, services/connectors/daily, services/alerts/{rules,dispatch}, web/routes_{runs,dashboard,statements,settings,sources,alerts,api_read} + repo.get_user_audit (the grep-missed EMAIL check). S-6 read tokens (rt_/at_) now scope to the token-owner's active workspace (routes_api_read + api_auth). STAY user-scoped (documented in-code): membership/idempotency/magic-link; AlertEvent/AlertCheck/Payment (no workspace_id column → O-1b); Subscription/entitlements/currency (billing model unruled, PLAN-ORG Q3 → O-1b/O-2); SavedView + developer tokens/OAuth apps (personal by uq/design); every MUTATE select (revoke/rename/purge/close/mint/feedback → fail-closed until O-2 RBAC). No migration (O-0 added + backfilled the columns). Engine untouched | tests/test_workspace_spine.py::TestReadScopeSweep (active_workspace_id ensure-creates + never None; get_user_audit workspace-scoped; NULL-workspace audit never matches a workspace read; re-scoped service read isolates across workspaces) + test_developer_platform read-API tenant isolation + full regression as the 1:1 equivalence oracle; tests/conftest fixture-stamp parity hook | (PLAN-ORG O-1) |
| O-1b-1 (R-ORG, PLAN-ORG — workspace switcher, founder "proceed as recommended" 2026-07-24) | C6,web | web/shell.py::workspace_bar (the topbar "acting in &lt;workspace&gt;" indicator + switcher options — active-workspace name + every membership with role/active flag; the ONE helper used by both _shell_ctx AND the manual-render pages so the bar is on EVERY app page), _shell.html topbar switcher (details/summary reusing the account-menu pattern; &gt;1 workspace → switch forms per non-active membership, solo → honest plain indicator with NO dead control per the shell's no-coming-soon rule), POST /settings/workspace/switch → repo.set_active_workspace (membership-validated; a forged/foreign workspace_id is a silent no-op; audit-logged when it takes; lands on /dashboard so the flip is visible), wired into routes_dashboard._shell_ctx + routes_sources (sources + connect_wizard) + routes_developer + routes_pages (upload). Switching flips EVERY owned read via the existing active_workspace_id chokepoint — no per-read change. Engine untouched | tests/test_workspace_spine.py::TestWorkspaceSwitcherJourney (shell shows active workspace + switcher; switching A→B flips every read incl. the dashboard's zero-state + freshness; switch to a non-member workspace refused and changes nothing; solo user gets the honest indicator with no switch control; indicator reachable on every app page incl. the manual-render sources/developer/upload) + mockup docs/design/mockups/workspace-switcher.html | (PLAN-ORG O-1b-1) |
| O-1b-2 (R-ORG, PLAN-ORG — invite &amp; accept, founder "proceed as recommended" 2026-07-24) | C6,C8,web,persistence | web/routes_members.py: GET /settings/members (invite form owner+Scale-only, else the honest reason — no dead control; pending-invites list with pending/expired), POST /settings/members/invite (OWNER-ONLY + Scale/team-gated + rate-limited 5/min; mints secrets.token_urlsafe(24) stored ONLY as credential_fingerprint HMAC; emails accept link via mail.workspace_invite; raw code never persisted), GET /invite/accept (confirm page — NEVER consumes, mail-scanner-safe; honest states ready/sign_in/wrong_email/invalid via soft auth so no raw 401), POST /invite/accept (single-use atomic UPDATE-where-unconsumed rowcount-checked + email-MATCH defense-in-depth + expiry; adds WorkspaceMember(role=member) + set_active_workspace → lands on /dashboard scoped to the shared workspace). mail.workspace_invite added to MailPort + LogMailAdapter + SmtpMailAdapter; repo.workspace_role + repo.list_workspace_invites; Members sidebar nav + Settings link + help_registry members purpose. Engine untouched | tests/test_workspace_invites.py (owner-invites→invitee-accepts→shared dashboard shows the owner's audit→third-party isolation; adversarial wrong-email/reused/expired all refused with invite left unconsumed; owner-only 403; Scale-gated 403; members surface honest states / no dead form; code stored as hash not plaintext) + /settings/members added to test_workspace_spine reachability walk | (PLAN-ORG O-1b-2) |
| Data coherence &amp; honest freshness (founder walkthrough 2026-07-24 — "no sources connected but overview shows old cache data") | C6,web | metrics.has_live_feed (active workspace has an active Source OR unrevoked IngestKey OR unrevoked Device — mirrors "connected" on the Sources page); shell.data_freshness (shared seam like workspace_bar: freshness + sources_disconnected + data_as_of; historical-but-disconnected → freshness gains "· nothing connected"), wired into _shell_ctx (8 pages) + the 4 manual full-page renders (sources, connect wizard, developer, upload) = every shell page; _shell.html coherence banner (on every app page: figures are from your last audit on <date>, won't change until you reconnect + Connect-a-source CTA). No new capability, no engine change — an honesty/coherence fix so historical figures never read as live (X-01 batch-audit, never a live proxy) | tests/test_data_coherence.py (healthy→no banner + live freshness; all sources revoked→banner on dashboard+sources+runs across both render paths + freshness flags it; new user no audit→honest "No data yet"; live ingest key counts as connected) | — |
| O-1b-3 (R-ORG, PLAN-ORG — members page &amp; revoke, founder "proceed O-1b-3" 2026-07-24) | C6,web,persistence | repo.list_workspace_members (the inverse of list_memberships — members OF a workspace joined to User for the email, owner-first then joined; each row has a real user). web/routes_members.py: GET /settings/members now renders the ROSTER (email · role · joined) visible to EVERY member, with a per-member Remove control that renders ONLY for an owner over a non-owner (can_revoke) — a plain member sees the list with NO revoke/invite control (ABSENT, reachability law for permissions, not a 403 bumped into), plus the honest "just you so far" solo empty state; POST /settings/members/{id}/revoke (OWNER-ONLY; deletes the WorkspaceMember — the switchable active_workspace_id resolver already falls a revoked member back to their PERSONAL workspace on the next request, so access stops with no extra step; guards: owner-of-this-workspace only, target must be a member OF this workspace else 404 = no cross-tenant reach, the owner row is never revocable = 400; audit-logged); POST /settings/members/invite/{id}/resend (OWNER-ONLY + Scale-gated + rate-limited 5/min; re-mints the one-shot code — overwriting the stored hash so the previous link dies instantly — and re-emails; only an unconsumed invite of this workspace); POST /settings/members/invite/{id}/cancel (OWNER-ONLY, no Scale gate — cleanup always allowed; deletes the pending invite so its link loads nothing → the same 'invalid' state). members.html extended with the roster + resend/cancel controls + status banners (removed/resent/canceled). Engine untouched; mutations owner-scoped fail-closed (O-2 RBAC still owns member-level roles) | tests/test_workspace_members.py (owner sees roster→revokes member→membership gone + resolver falls back + shared audit no longer visible; revoke control ABSENT for a plain member AND route owner-only 403; owner row 400; foreign/unknown member id 404; solo empty state; resend rotates the code so the old link dies and the new one is live; cancel withdraws the invite and kills the link; resend/cancel owner-only 403) | (PLAN-ORG O-1b-3) |
| O-2 RBAC (R-ORG, PLAN-ORG — roles over PRODUCT actions, founder "merge and start LE-3" → chose O-2 per ROADMAP §3 #1, 2026-07-25) | C6,web,persistence | web/authz.py — the role→permission matrix at the WEB boundary (Perm enum + can/can_manage_member/assignable_roles/ensure/perms_context; owner=all, admin=govern-minus-money/ownership, member=run-not-govern, viewer=read; fail-closed on unknown/None role; the audit engine NEVER imports it — T-NFR-01 intact); repo.active_role (the caller's role in their ACTIVE workspace — the one datum the gate needs); ROUTE-BOUNDARY enforcement replacing the ad-hoc inline `workspace_role==owner` — routes_members invite(+role param)/revoke/resend/cancel + NEW POST /settings/members/{id}/role (MANAGE_MEMBERS; admin never over the owner, never self), routes_sources connect/validate/revoke + routes_ingest sdk mint/revoke + routes_devices link/revoke (MANAGE_SOURCES), api/routes_upload create_audit (RUN_AUDITS — viewer blocked past the payment gate); RENDERED-surface gating via perms_context in _shell_ctx + the manual sources_page/billing renders so a role never SEES a control it can't use (members governance controls, sources connect/revoke + SDK/device mints, billing owner-only); billing visibility now owner-only (resolves the models.py O-2 note). Single-tenant unchanged (a solo user is owner of their workspace-of-one). Engine role-blind | tests/test_authz.py (every matrix cell pinned incl. fail-closed None/unknown, can_manage_member never over the owner, assignable never owner, ensure→403) + tests/test_rbac_journey.py (four roles in ONE workspace: governance/sources/billing controls visible only to the right roles; member/viewer invite/connect/set-role POST → 403 fail-closed; viewer can't run even WITH a credit, member can) + mockup docs/design/mockups/o2-rbac.html | (PLAN-ORG O-2) |
| R-REACHABILITY #2 — in-app View report (ROADMAP §3 #2, founder "approved and proceed" 2026-07-26) | C5,web | web/routes_runs.view_report — GET /audits/{id}/report: authenticated, verifies the audit is in the caller's ACTIVE workspace and status=done (VIEW is universal — every role incl. viewer may read a report; a foreign/guessed id or a not-done audit → 404, no cross-tenant leak), mints a FRESH sign_report_url token and 303-redirects to the existing /r/{token} page — ONE report-rendering path, now reached in-app in one click. _runs_ledger.html gains a "View report" link per done run (report_ready flag). Closes the gap where a report was reachable ONLY via the emailed signed link (a customer who closed the email had no way back). No engine change | tests/test_view_report_reachability.py (runs ledger links the report; click-through 303→/r/{token}→200 renders the report; stranger 404; not-done 404; unknown id 404) | api/overview |
| §3 #3 report plain-English parity (REPORT track, FR-14/FR-16; founder "merge and continue once green" 2026-07-26) | C4,C5,web | services/rules/detector_copy.py — the SINGLE SOURCE of the plain-English detector copy (plain/summary/why/fix/verify per detector), moved VERBATIM from web/help_registry.yaml to the SERVICES layer so BOTH the in-app /findings AND the downloadable report render the SAME words; the audit engine stays network/LLM-free (T-NFR-01 — pure yaml→dict data). web/help.py detector()/detector_plain/detector_summary/detector_keys now read detector_copy (in-app copy byte-identical, new home); help_registry.yaml drops its detectors section (no dual source, no drift). services/report/render_pdf.render_report_html passes dcopy=detector_copy into the render context; _report_body.html renders each finding's plain headline + summary (shared by web report.html AND pdf/report.html). No new estimator/route → no golden, no OpenAPI change | tests/test_report_web.py::TestReportPlainEnglishParity (one source serves both consumers — plain+summary identical across web.help and detector_copy for every detector; the rendered report shows each finding's escaped plain+summary) + existing report/findings/first-run/explorer copy consumers green | report/reading-a-report |
| Guided first run &amp; output preview (founder walkthrough 2026-07-25 — punch-list #4/#5) | C5,C6,web | services/report/sample.sample_model (memoised ReportModel accessor — the FR-16 sample exposed STRUCTURALLY, no WeasyPrint render); services/dashboard/metrics.first_run_preview (the sample headline savings/%/spend + impact-ranked top-N findings as {detector,severity,monthly_usd}; $0 rows excluded, honest n_findings kept; detector→plain mapped at RENDER time via the help registry so the engine stays presentation-blind; X-04 untouched — deterministic, no LLM narrative; FR-22 — counts only); web/routes_dashboard.dashboard (first_run = no completed audit via latest_audit; the preview is assembled ONLY then and FileNotFoundError-degrades to the hero alone — never a 500; the getting-started checklist is suppressed in first run so the guided hero is the single next-action surface); templates/app/_first_run.html (the guided 3-step "start here" hero with step-1 "YOU ARE HERE" + the SAMPLE-fenced output preview composing the kit .ledger — the SAME findings table the real dashboard renders); dashboard.html first-run branch (pipeline spine always, then hero+preview XOR the live widget grid); wa-design.css .fr-* (estimate-palette SAMPLE fence, role-tokens only, no retired serif). HONESTY (data-coherence law): the sample is fenced as "SAMPLE — NOT YOUR DATA / nothing on this card is yours", uses the estimate palette never verified-green, and VANISHES the moment a real audit completes so it never sits beside the user's own figures. Extends FR-16's sample report into the in-app first run. REV 2/3 (founder walkthrough on PR #17): Upload-a-log/SDK is the PRIMARY first-run action (per-request runs all 6 detectors PER ROUTE = the most findings; connect-a-provider is the secondary automatic-but-coarser path — aggregate usage runs only 3 of 6 detectors — with the depth trade stated honestly); plain-English finding SUMMARIES — help.detector_summary + DetectorHelp.summary + a `summary` per detector in help_registry.yaml + i18n kit.finding.summary "In plain English" — LEAD every finding while the technical pointers (detector id/thresholds/evidence) stay present (both audiences, plain-first) across the drawer (prepended, R-CLARITY depth order preserved), Findings page, dashboard top-findings, and the first-run preview cards; the PDF report's plain-English is a services-layer fast-follow (render-only, no help access). Engine untouched | tests/test_guided_first_run.py (new user lands in the guided hero; both CTAs reach live 200 routes incl. /sample; the preview shows the REAL engine sample — savings/% + EVERY previewed finding's plain title, HTML-escaped as rendered, read from the same function the template uses; the SAMPLE fence is unmistakable AND the user's own freshness stays "No data yet" with no fabricated $0.00; hero+preview vanish once a real audit exists → live w-savings + the user's OWN finding) + tests/test_onboarding.py::TestOnboardingOnDashboard (checklist suppressed in first run, resumes after the first audit) + tests/test_dashboard.py::test_02 (zero-state updated to the guided hero) | mockup docs/design/mockups/guided-first-run.html |
| Richer findings — D8/D9 detectors (founder 2026-07-25, "many findings / dynamic analysis") | C4 | services/rules/d8_spend_concentration (informational "start here" — flags a route >= d8_concentration_min_share of spend across 2+ named routes; $0 impact, like d5) + d9_ineffective_cache (cache written but rarely read = net cost; money-math on OBSERVED billed cache_write/cached tokens so CONSERVATIVE; DISJOINT from d2 by construction — d2 filters cache_write_tokens==0 — so savings never double-count); registry DETECTORS + both added to aggregate.INACTIVE_ON_AGGREGATE (per-request only: d8 needs route tags, d9 needs cache_write counts absent from provider aggregates); config d8_concentration_min_share/d9_min_cache_write_tokens; help_registry entries (plain/summary/why/fix/verify) — surface through the existing finding UI (plain-English + technical). Engine stays pure (T-NFR-01), no network/LLM | tests/test_rules.py::TestD8SpendConcentration (dominant route flagged informational, single-route/even-split/untagged silent) + ::TestD9IneffectiveCache (golden net_loss 0.3075 derived in fixtures/pricing_golden_NOTES.md D9 section — CLAUDE.md rule 4; net-positive/no-write-premium silent; disjoint-from-d2) + registry-order/detector-count/aggregate-coverage updated | concepts/waste-classes (pending) |
| Tokenomics breakdown — enterprise depth engine slice 1 (founder 2026-07-25, "enterprise-ready, industry-standard, deterministic — no LLM in the money path") | C4,C5,web | services/dashboard/tokenomics (PURE deterministic breakdown of a priced frame: vitals — spend/tokens-in-out-cached/cache-hit/out:in/cost-per-1k-out — + per-dimension Slices by model and by route/tag (cost allocation/showback) + unit economics + data coverage %priced/%attributed; every figure an exact sum/ratio of already-priced cost_usd, NOT a new estimator so no rate golden owed, but the money is pinned by tests/test_tokenomics.py hand-derived values; unpriced rows excluded from money but counted in coverage — honest completeness); runner writes the tokenomics.json ARTIFACT at audit time from the per-request frame (purged later, FR-21 — computed once, like spend_by_model); web/routes_dashboard GET /breakdown reads the latest audit's artifact (honest empty state / unpriced-clarity when absent); templates/app/breakdown.html (kit-composed vitals stat-tiles + by-model + by-route cost-allocation ledgers + data-coverage); shell nav "Breakdown" in the Monitor group + help_registry breakdown destination. Exact + reproducible + private (FR-22 counts-only) = the audit-grade moat an LLM tool cannot match. Engine pure (T-NFR-01) | tests/test_breakdown.py (real audit → tokenomics.json artifact → /breakdown renders vitals + by-model + by-route + coverage with the exact slices; honest empty state; reachable from shell nav) + tests/test_tokenomics.py (vitals/slices/unit-econ/coverage math hand-derived; untagged-attribution; unpriced-coverage; empty-frame) | concepts/tokenomics (pending) |
| Cross-audit efficiency drift — depth engine, "vs your last audit" (founder 2026-07-25, "proceed as recommended next step" — the drift half of anomaly &amp; drift) | C5,web | services/dashboard/drift (PURE deterministic diff of two tokenomics.json vitals: cost-per-request/cost-per-1k-output DOWN=better, cache-hit UP=better, monthly spend=CONTEXT no verdict; sub-material change=flat; regression list drives the callout; no new estimator so no golden owed); metrics.recent_done_audits (2 most recent DONE audits, workspace-scoped); routes_dashboard._load_tokenomics + breakdown_page (loads current+prior tk, computes drift; needs BOTH artifacts or no trend — honest, never half a comparison, never a 500 on corrupt/missing); breakdown.html "Trend vs your last audit" kit table (current/prior/change/trend, role-token verdict colours inline) + regression callout + honest run-another empty state. Deterministic, FR-22 counts-only | tests/test_drift.py (better/worse/flat/context/spend-down-not-better/prior-zero-guard, hand-derived deltas) + tests/test_drift_journey.py (two real audits → trend + regression callout; single audit → honest empty state) + tests/fixtures/drift_prior.csv + drift_current.csv | concepts/tokenomics (pending) |
| Findings clarity — materiality floor + route naming (FR-13; founder 2026-07-25, "work through what is worth fixing, largest dollar impact first") | C4,web | services/rules/registry.run_all materiality floor (drop a SAVINGS finding when 0 &lt; monthly &lt; min_finding_monthly_usd — it renders as $0.00, noise; an INFORMATIONAL pointer sets monthly to EXACTLY 0.0 and is always kept — the 0.0-vs-strictly-positive invariant needs no allowlist); config min_finding_monthly_usd ($0.005) + .env.example; d1/d2/d3/d9 Finding detail["route"] (model or tag); runner FindingRow.route from detail.route‖model; web/routes_dashboard /findings items +route; templates/app/findings.html row renders "on &lt;route&gt;" so same-kind findings read DISTINCT. Money values UNCHANGED (no golden — only drops $0-noise + names findings), FR-22 counts-only | tests/test_rules.py::TestFindingsClarity (floor drops savings/keeps informational via huge-floor probe; default floor keeps all 6 waste_pack findings; d1/d2/d3 findings name their route) | report/reading-a-report |
| FR-33 (Spend anomaly — D10 detector, depth-engine "dynamic analysis"; amendment 2026-07-25, R-DEPTH-ENGINE, founder "proceed next" → "Anomaly &amp; drift detection") | C4 | services/rules/d10_spend_anomaly (deterministic robust temporal spike detection over the audit's OWN daily-spend series: median + MAD not mean + std, so a spike cannot inflate its own baseline; TWO scale-free gates — statistical z>=d10_z_threshold MADs (or, on a flat baseline, carried by materiality) AND materiality >=d10_spike_mult x median — over >=d10_min_days; the earlier excess-share-of-total gate was DROPPED in the gate round as window-length-dependent + redundant; self-referential (temporal), so a legitimately heavier route/model is never mistaken for a spike; severity is anomaly-native by the deviation MULTIPLE (>=10x HIGH/>=4x MED/else LOW), not the monthly-USD scale; INFORMATIONAL $0 claimed like d8 — no invented saving for an unnamed spike, complementary to the pattern detectors; attributes the top driver model+route; honestly DORMANT below the weekly baseline); registry DETECTORS + aggregate.INACTIVE_ON_AGGREGATE (+d10 — per-request only; aggregate prices buckets itself); config d10_min_days/d10_z_threshold/d10_spike_mult + .env.example; help_registry d10 entry (plain/summary/why/fix/verify) + help._threshold_values (+3 placeholders); _finding_drawer.html shows an honest "Informational" chip (not $0.00 waste) for $0-impact findings (D5/D8/D10) — surfaces through the EXISTING finding UI (Findings page + drawer, plain-English + technical, no new route). NO new rate/estimator so no golden owed — pinned by RATE-INDEPENDENT hand-derived multiples. Engine pure (T-NFR-01), FR-22 counts-only | tests/test_rules.py::TestD10SpendAnomaly (rate-indep golden multiple, driver attribution, dormancy &lt; min-days, flat-silent, robust-baseline TWO-masking-spikes a mean detector misses, each gate active, severity-by-multiple, untagged flags on model, chronological ids, short-real-fixture dormant, empty/all-unpriced/median&lt;=0 guards) + tests/test_spend_anomaly.py (journey: 7-day spike fixture → real audit → reachable /findings + drawer plain-English for both audiences; short-log dormant) + tests/fixtures/spend_spike.csv; test_dashboard help-count 8→9, test_source_audit coverage 8→9, test_aggregate_rules INACTIVE list | concepts/waste-classes (pending) |
| R-PLATFORM slice 1 (Issue #52, founder-labelled `loop:ready` 2026-07-27) | C1 | docs-site/api/reference.md — NEW "Run an audit (upload)" section documenting `POST /api/v1/audits` (payment gate FR-18, RUN_AUDITS role gate, multipart request, curl+Python examples, full response/error table) + a full error catalog (trigger + fix per 400/401/402/403/404/413/422/429/500, cross-referenced to the endpoints that raise each); docs-site/api/getting-started.md (NEW — mint key→first call→see it land, 4 steps); docs-site/api/tutorial.md (NEW — runnable send-usage→poll-status→read-findings walkthrough, curl+Python); mkdocs nav rows for both. No product code changed — docs + the doc-coverage test only (out of scope per the issue) | tests/test_docs_site.py::TestApiReferenceAccuracy::test_12_every_current_v1_endpoint_documented_by_method (verb-aware doc-coverage guard — every declared `/api/v1` endpoint's real route is pinned AND its `METHOD path` string is confirmed present in reference.md) + TestDeveloperGettingStartedAndTutorial (both pages in nav, <=5-step cap, real endpoints named, tutorial covers ingest/status/findings) | api/getting-started, api/reference, api/tutorial |
| R-PLATFORM slice 2 (Issue #54, PLAN-SDK S-3, founder-labelled `loop:ready` 2026-07-27) | C1 | mcp/server (stdio JSON-RPC 2.0 MCP server — `initialize`/`tools/list`/`tools/call`; the two tools `list_audits` scope `read:audits` and `list_findings(audit_id)` scope `read:findings` are a thin urllib GET against the EXISTING `web/routes_api_read` endpoints — no query-logic duplication, tenancy/scope enforcement stays server-side exactly as for any other `rt_…` caller; auth via `TOKENOPS_COST_AUDITOR_TOKEN` env var (matches the reference.md convention) + `TOKENOPS_COST_AUDITOR_SERVER` override; missing token short-circuits before any network call; a non-2xx response (401/403/404) surfaces the server's own NFR-14 envelope message as an `isError` tool result, never a crash; read-only — no write tools this slice), mcp/__main__ (`python -m tokenops_cost_auditor.mcp`), cli.py `mcp` subcommand (`tokenops-cost-auditor mcp`); docs-site/api/mcp.md (install, Claude Desktop/Cursor config snippets, the two tools) + mkdocs nav row. FR-22 by construction — the tools return exactly what routes_api_read returns (counts/dollars), never text. Engine untouched (T-NFR-01 — mcp/ is outside services/rules and services/pricing) | tests/test_mcp.py (tools/list shape incl. inputSchema + required fields; initialize serverInfo; tools/call happy path for both tools incl. the real query string/Authorization header sent; missing-token never hits the network; invalid-token 401 and scope-denied 403 and foreign-audit 404 all surface the server's message as isError; unknown tool/method are JSON-RPC errors; a notification gets no reply; full stdio-loop round trip over StringIO) + tests/test_docs_site.py::TestMcpDocs (in nav, token/server env vars + both launch commands documented, both client configs present, tool table matches the real TOOLS list + scopes, read-only/no-write-tools stated, FR-22 counts-only stated) | api/mcp |
| R-PLATFORM slice 3 (Issue #56, PLAN-SDK S-5, founder-labelled `loop:ready` 2026-07-27) | C1,web,persistence | persistence/models.py `WebhookEndpoint` (workspace-scoped outbound target; `secret` plaintext — unlike a credential we only verify, we must hold it to SIGN every delivery — NULLed on revoke alongside `active=False`, authority law) + `WebhookDelivery` (endpoint_id/event/status_code/attempted_at, one row per best-effort attempt); migration 022 (additive, two tables); services/webhooks/base.py (`sign` — HMAC-SHA256 hex digest, the SAME algorithm `api/routes_webhooks.verify_signature` checks inbound, so the scheme is symmetric — + the `WebhookPort` protocol) and services/webhooks/dispatcher.py (`WebhookDispatcher` — best-effort, `TIMEOUT_S=5.0`, ONE attempt per active endpoint, catches its own exceptions so a delivery failure can never fail the audit it reports on; no durable retry queue this slice, out of scope per the issue); services/runner.py `AuditRunner.webhooks: WebhookPort` field, dispatched right after `mail.report_ready` on audit completion with an FR-22-clean payload (audit_id/workspace_id/status/total_spend_usd/projected_spend_usd/savings_pct/finding_count/findings\[{detector,severity,monthly_usd}\] — never prompt/completion text); web/routes_webhooks.py — Settings → Webhooks (`/settings/webhooks` list/add/delete, O-2 `MANAGE_SOURCES` gate mirroring routes_sources.py exactly: list visible to every role, add/remove controls absent for non-managers, forged POST 403s); templates app/webhooks.html + app/_webhook_secret.html (shown-once secret reveal, the read-token grammar) + `_settings_nav.html` Webhooks tab; docs-site/api/webhooks.md (payload schema, HMAC verification snippet, best-effort/no-retry-queue disclosure) + mkdocs nav row. X-01/X-02 SAFE — observe-and-notify only, never in the request path, never enforces. Engine untouched (T-NFR-01 — services/webhooks is outside services/rules and services/pricing) | tests/test_webhooks_journey.py (TestWebhookJourney: add endpoint via the real route → run a real audit → the signed delivery's body/headers are captured and independently re-verified with the shown-once secret → FR-22 marker-absence check → WebhookDelivery row recorded → the Settings page shows the endpoint and "200 ok"; a raising transport records a null status_code and the audit still reaches status=done, never raised; TestWebhookRBAC: add/remove controls absent for a member + honest "managed by owners and admins" note, forged add/delete POSTs 403 fail-closed) + tests/test_docs_site.py::TestWebhooksDocs (in nav, real header/event constants documented, hmac.new/hmac.compare_digest snippet present, FR-22 + best-effort/no-retry-queue stated) | api/webhooks |
| R-PLATFORM slice 4 (JS/TS SDK, PLAN-SDK S-4, founder "continue with the SDK slice" 2026-07-27) | C1 | sdk/js — the official TypeScript SDK (npm `tokenops-cost-auditor`): a `TokenOps` client with counts-only-BY-CONSTRUCTION `UsageRecord` (no prompt/completion text field, so a caller cannot send text — FR-22), a two-credential/two-capability split (ingest key `ik_…` writes via POST /api/v1/ingest; read token `rt_…` reads via GET /api/v1/audits + /audits/{id}/findings — NO new server-side capability, calls only existing endpoints so tenancy/scope enforcement stay server-side), zero runtime deps (platform fetch), `TokenOpsError` surfacing the NFR-14 envelope, `waitForAudit` polling List-audits; mirrors the Python SDK's laws (S-0/S-1), observe-only X-01/X-02 safe. .github/workflows/sdk-js.yml (setup-node CI: strict tsc typecheck + node --test + tsc build on any sdk/js change — a Node toolchain separate from the Python app, which is unaffected). docs-site/api/sdk-js.md + mkdocs nav. Engine untouched (T-NFR-01 — sdk/js is a separate npm package outside services/rules + services/pricing) | sdk/js/src/index.test.ts (ingest POSTs to /api/v1/ingest with the ingest-key Bearer; listFindings uses the read token; a non-2xx raises TokenOpsError with .status + the envelope message; calling a capability without its credential throws before any request) + .github/workflows/sdk-js.yml (typecheck+test+build) + tests/test_docs_site.py::TestSdkJsDocs (in nav; install + both credentials + the real endpoints/scopes documented; counts-only/FR-22 stated) | api/sdk-js |
| R-PLATFORM slice 6 (GitHub Action) | C1 | action/ — a node20 GitHub Action that posts the latest COMPLETED audit's findings (ranked by monthly $) to `$GITHUB_STEP_SUMMARY` and optionally fails the job on a severity threshold (`fail-on-severity` low/medium/high); dependency-free (platform fetch + node:fs), READ-ONLY via a read token (`rt_…`), calls ONLY GET /api/v1/audits + /audits/{id}/findings — no new server-side capability; observe-only X-01/X-02 safe, FR-22 (counts/$/detector metadata, never prompt/completion text). action.yml (`using: node20`, `main: dist/main.js`) + src/main.ts (`run(deps)` with injected fetch/env/writeSummary/fail so the whole run is unit-testable; entry-guarded so importing it in a test never auto-runs) + tsconfig/tsconfig.build/package.json; dist/main.js is COMMITTED (a GitHub Action runs its dist directly, no install step). .github/workflows/sdk-js.yml generalized to a matrix over [sdk/js, action] (setup-node: strict tsc typecheck + node --test + build). docs-site/api/github-action.md + mkdocs nav. Engine untouched (T-NFR-01 — action/ is outside services/rules + services/pricing) | action/src/main.test.ts (writes a ranked findings summary for the latest done audit incl. the total; fail-on-severity=high fails when a high finding exists; missing read-token fails before any request — all via injected fetch/env) + .github/workflows/sdk-js.yml (matrix typecheck+test+build) + tests/test_docs_site.py::TestGithubActionDocs (in nav; usage/inputs + the real endpoints/scopes documented; read-only/FR-22 stated) | api/github-action |
| R-PLATFORM slice 5 (Issue #59, founder-labelled `loop:ready` 2026-07-27) | C1,web,persistence | Usage VISIBILITY (not limits/enforcement — X-01/X-02 unchanged) for both credential kinds authenticating `/api/v1`: persistence/models.py `IngestKey.request_count` + `ApiToken.request_count` (INTEGER NOT NULL DEFAULT 0, alongside the existing `last_used_at`); migration 023 (additive, two columns, `server_default="0"`); web/api_auth.py `_bump_usage` (rt_ path, called from `resolve_read_bearer`) and web/routes_ingest.py `_bump_usage` (ik_ path, called from `POST /api/v1/ingest`) — each wrapped in its own `try/except Exception` so a metering-write failure logs a warning and NEVER fails the API response it meters (best-effort by design, chosen over a per-day `KeyUsage` bucket table for one cheap UPDATE per request); web/routes_developer.py `token_view` carries `request_count`; templates app/developer.html (API tokens table — new "Requests" column) + app/sources.html (SDK & API ingest-key card — new "Requests" column), both with an honest "never"/zero state for a fresh credential. Engine untouched (T-NFR-01 — this lives entirely at the web/persistence edge); no prompt/completion text anywhere (FR-22 — counts and timestamps only) | tests/test_ingest_api.py::TestUsageMetering (each POST increments the key's request_count + stamps last_used_at; a fresh key shows honest zero/never on Sources; monkeypatching `routes_ingest._bump_usage` to raise still returns 201 and the audit still completes) + tests/test_developer_platform.py::TestUsageMetering (each read increments the token's request_count; a fresh token shows honest zero/never on Developer Settings; the "Requests" column renders the real count; monkeypatching `api_auth._bump_usage` to raise still returns 200 with the real audits) | — |
| R-NAME-SEO §2 (Issue #62, founder-labelled `loop:ready` 2026-07-27) | web (templates only) | The SEO/copy law — no public page markets bare "TokenOps" without an AI/LLM/cost qualifier ("Cost Auditor", "AI", "LLM", "spend", "cost"). Fixed the one live gap: `landing.html`'s `og:title` ("TokenOps Cost Auditor — by WitAura") had no explicit AI/LLM word beyond the brand name — now "TokenOps Cost Auditor — AI spend audits, by WitAura", same meaning. Copy-only; no rename, no new page, no engine change | tests/test_seo_copy.py — `TestPublicRoutesRendered` hits `/`, `/sample`, `/legal/{terms,privacy,dpa}`, `/login`, `/signup` through the real app and checks every title/meta[description]/og:title/og:description that names "TokenOps" also carries a qualifier (pins the landing fields specifically); `TestEveryPublicTemplateSource` sweeps every template outside `app/` (authenticated) and `kit/` at the source level (oauth consent/error, web+PDF report, invite/verify confirm) so a new public page or field can't ship unqualified without a live route | — |
| R-CATEGORY §2 / R-NAME-SEO §4 / R-COMPETITIVE-LEARN §3 (Issue #64, founder-labelled `loop:ready` 2026-07-27) | web (templates only) | Landing comparison strip (`landing.html` `.land-compare`, ~L309-327) enriched with the honest category framing from three rulings — CATEGORIES only, never vendor product names. Added the missing rows (FinOps platforms, Consultancy audits, Observability) alongside the existing Model routers / Gateways &amp; proxies / TokenOps rows. FinOps-platforms row carries the real cost (a tagging program + an enterprise contract) vs the TokenOps row's self-serve, zero-integration, evidence-cited contrast. A new `hero-note`-styled line states the GPU-scope boundary plainly: managed API token spend is audited, GPU/training-compute cost is out of scope. Copy-only; no new page, no rename, no engine change | tests/test_seo_copy.py::TestComparisonStrip — every category label renders; the FinOps-platforms row names tagging+contract; the TokenOps row names self-serve; the GPU-scope line renders near the strip; a short vendor-brand denylist confirms none appear in the strip | — |
| R-PRICING-PAGE (Issue #66, founder-labelled `loop:ready` 2026-07-27) | web (routes_pages, templates) | NEW `GET /pricing` route (`routes_pages.py::pricing_page`) + `templates/pricing.html` — a dedicated, clean pricing page rendering the EXISTING plan config (`services/payments/plans` catalogue, R-PRICING-FINAL-2) as ONE effective price per plan per region, instead of the landing's mixed launch/list/anchor jumble. Region-aware via `plans.viewer_currency` + `plans.launch_open` (the same functions landing/billing already use — no reimplementation); per-plan checkout CTA is config-gated (`settings.checkout_link`), honest "Checkout opens once billing is switched on." note when a link is absent — never an invented link (same law as `/billing`). Reachable from the public nav + footer ("Pricing"/"Plans" now link to `/pricing`, `_public_shell.html`) and a new landing CTA ("See full pricing →" in the plans section). Display-only: no price VALUE changed, no golden-file/`pricing_verify` impact | tests/test_pricing_page.py (USD/INR effective-price rendering off the catalogue, no-jumble plan-card count, launch-note counts matching config not literals, currency toggle, one-shot price, nav+landing-CTA reachability, checkout-link honesty configured/unconfigured), tests/test_pricing_final.py::TestConfigOnlyLaw (repo-wide inline-price-literal sweep already covers the new template), tests/test_seo_copy.py (`/pricing` added to `TestPublicRoutesRendered.PAGES`; `TestEveryPublicTemplateSource` sweeps it automatically as a new top-level template) | — |
| Server-side geo detection (Issue #68, founder-labelled `loop:ready` 2026-07-27) | web,config | NEW `services/geo/resolver.py::country_for_request(request, settings) -> str \| None` — a trusted proxy header (`settings.geo_country_header`, default `CF-IPCountry`, set by Cloudflare's free tier) first, else a GeoIP lookup on the real client IP (`X-Forwarded-For`'s first hop, else `request.client.host`) against a DB-IP Lite Country `.mmdb` (free, CC-BY, no license key) via `maxminddb` IF `settings.geoip_db_path` is set and the file exists, else `None` — never throws, a miss is always `None`. `services/payments/plans.pick_currency`/`viewer_currency` swap their old `accept_language: str` parameter for `country: str \| None` at the SAME precedence slot (explicit `?ccy` toggle &gt; persisted toggle cookie &gt; server-side geo &gt; USD), wired at all four render call sites (`routes_pages.landing`/`pricing_page`, `routes_billing.billing_page`, `routes_alerts.alerts_page`); `services/connectors/daily` (no request context) is unaffected. RIPPED OUT the old browser guessing this replaces: the timezone→cookie detection `&lt;script&gt;` in `_public_shell.html` and the `"-in" in accept_language` branch in `pick_currency` — the explicit `?ccy` toggle is untouched. Config: `geo_country_header` (default `CF-IPCountry`) + `geoip_db_path` (default `""`, header-only) in `config.py` + `.env.example`; `maxminddb` added to `pyproject.toml`. `scripts/provision.sh` step 4d downloads the free DB-IP Lite Country db to `./geoip/` (a live bind mount, `docker-compose.yml` `GEOIP_DB_PATH=/geoip/dbip-country-lite.mmdb`) on every provision run — zero founder step; a failed/absent download leaves the header path fully working (never fails the deploy). Engine untouched (T-NFR-01 — `services/geo` is outside `services/rules`/`services/pricing`) | tests/test_geo.py (`TestHeaderPath`: `CF-IPCountry` IN/US, no-signal→`None`, the Cloudflare `"XX"` unknown sentinel treated as a miss, configurable header name; `TestGeoipDbPath`: a mocked reader resolves a country, `X-Forwarded-For`'s first hop wins over the proxy's own `client.host`, the header wins over the db, a missing/corrupt db file degrades to `None` not a crash, a reader exception on a malformed IP is a miss) + tests/test_pricing_final.py::TestOneCurrencyPerView (`CF-IPCountry: IN` renders `$4.99/mo` and `US` renders `$19/mo` through the REAL `/` route on first paint with no cookie; no-signal defaults to USD; the `ccy` cookie still persists a prior explicit toggle) | — |
| R-CCY-TOGGLE (Issue #70, founder-labelled `loop:ready` 2026-07-27) | web (payments/plans, routes_pages, routes_billing, templates) | The visible USD\|INR override for #68's geo auto-detect: a segmented `kit.ccy_toggle` macro (`kit/_kit.html`) on `/pricing` and `/billing`'s plans section — clicking a currency navigates with `?ccy=` and the server sets the `ccy` cookie on that response so the choice persists (billing was the one of the three render sites still missing the cookie-set the removed timezone-cookie JS used to do; landing/pricing already had it from #68/#66). NEW `plans.locked_currency(session, user_id) -> str \| None` extracted from `viewer_currency` — a CURRENTLY subscribed account (status active/past_due/read_only) is locked to its subscription currency; `viewer_currency` is now a thin wrapper. Honesty-rail fix folded in (R-IMPROVISE): a CANCELLED subscription row survives lifecycle-wise but the account is back on Free, so it must NOT read as locked — the old `viewer_currency` ignored status and would have locked a cancelled account's toggle forever, which the new static "billed in &lt;ccy&gt; on your plan" note would have made a visible lie; `locked_currency` excludes `status == "cancelled"`. `routes_pages.pricing_page` + `routes_billing.billing_page` both compute `locked` and pass `currency_locked` to their templates; `kit.ccy_toggle` renders a live two-option segmented control when unlocked, or `locked`'s static text (never a link) when locked — the honesty rail from the issue. No price VALUE changed; `?ccy` still wins over the cookie and over geo (precedence unchanged) | tests/test_pricing_page.py::TestPricingPageCurrencyToggle (toggle click redraws money off the catalogue + sets the `ccy` cookie; the cookie persists the choice on a later request with no `?ccy`; an explicit `?ccy` still overrides a prior cookie) + tests/test_subscriptions.py::TestBillingCurrencyToggle (same three for `/billing`, the one surface that was missing the cookie-set; a subscribed account sees the locked static note and never the live toggle markup even with a stray `?ccy`; a CANCELLED subscription is NOT locked — the toggle works again) | — |
| R-PLATFORM slice 7 (Issue #72, founder-labelled `loop:ready` 2026-07-27) | C1,web | `web/routes_api_read.py` NEW `GET /api/v1/audits/{audit_id}` — the audit's own summary object (`id, status, scope_label, created_at, completed_at, totals{calls, input_tokens, output_tokens, total_tokens, estimated_cost_usd}, model_count, finding_count`); SAME `read:audits`-scoped `ReadPrincipal` dependency and `active_workspace_id` tenancy path as the existing `GET /api/v1/audits` — no new auth mechanism, other-workspace id is 404 not 403. Totals summed from `CallAggregate` (calls/prompt_tokens/completion_tokens, model_count via distinct model), `estimated_cost_usd` read straight off `Audit.total_spend_usd` — the SAME figure the report shows, no parallel computation. An audit not yet `done` has no `CallAggregate` rows yet, so totals come back honestly zeroed/`null` rather than a 500. Counts/dollars only (FR-22) — no prompt/completion text exists to return. No SDK method this slice (out of scope per the issue) | tests/test_developer_platform.py::TestGetAuditSummary (a real upload→run pipeline audit's summary totals independently cross-checked against its own `CallAggregate` rows AND the report.json `total_spend_usd`; cross-workspace id → 404; token missing `read:audits` → 403; a `running` (incomplete) audit returns zeroed/null totals, never 500; FR-22 shape — response keys are exactly counts/meta/totals, no prompt/completion/content substrings) | api/reference |
| Razorpay Standard Checkout for the one-shot INR audit (Issue #74, founder-labelled `loop:ready` 2026-07-27) | C7,web | NEW `services/payments/razorpay_orders.py` — `create_order` (httpx `POST /v1/orders`, HTTP Basic Auth `key_id:key_secret`, amount in paise, `notes.email`, injectable client for tests mirroring `services/connectors/base.py::SupportsGet`) + `verify_order_signature` (HMAC-SHA256 of `"{order_id}|{payment_id}"` with `key_secret`). `services/payments/razorpay_link.py` gained `OrderWebhookEvent` + `parse_order_event` — `order.paid`/`payment.captured` (both mean "paid", either may arrive first) and `payment.failed`, on the SAME FR-27 signature+timestamp-tolerance rails as `payment_link.paid`. `api/routes_webhooks.py` `_credit_order`/`_handle_order_event` — the webhook is the SOLE credit-grant authority (B1), idempotent on `Payment.ref == order_id` (B3) in addition to the existing event-id dedup, so `order.paid` + a same-order `payment.captured` (different event ids) or a re-delivered `order.paid` collapse to exactly one credit; `payment.failed` is an honest no-credit path (B4), never a 500. `web/routes_billing.py` NEW `POST /billing/razorpay/order` (creates the order server-side FIRST — a payment with no order_id auto-refunds; O-2 `MANAGE_BILLING`-gated, 503 "checkout not switched on" when key_id/secret unset) + `POST /billing/razorpay/verify` (handler-payload signature check — UX only per B1, never itself grants; 400 on mismatch). `billing_page` now sets a `Content-Security-Policy` header (script-src/frame-src `checkout.razorpay.com`, connect-src `api.razorpay.com`) — the modal is an iframe and needs it. `templates/app/billing.html` — "Buy one-time audit" button for INR viewers only (USD stays on the existing Stripe hosted link, unchanged) wired to `checkout.razorpay.com/v1/checkout.js`, honest disabled note when unconfigured, honest failure/dismiss states (B4, never a limbo screen). `razorpay_key_id`/`razorpay_key_secret` (existing but previously-dead config) now USED. Out of scope, parked to `docs/internal/BACKLOG.md`: a `callback_url` fallback for iframe-blocked browsers (B5) | tests/test_razorpay_checkout.py — `TestOrderCreate` (mocked httpx boundary: amount-in-paise/currency/Basic-Auth-tuple/notes.email request shape, `order_id`+`key_id` in the response, provider-error surfaces 502), `TestSignatureVerify` (real HMAC valid→200/UX-only-no-credit-yet, tampered→400, key-secret-unset→503), `TestFullJourney` (create-order→handler-verify(200,no grant yet)→signed `order.paid` webhook grants THE credit→reflected in the user's state via a real upload unlock; tampered handler signature→400 and grants nothing), `TestIdempotency` (handler path + a re-delivered `order.paid` grants exactly once; `order.paid` + `payment.captured` for the same `order_id` collapse to one credit; `payment.failed` grants nothing), `TestHonestState` (billing page shows the disabled note and no button when unconfigured, the CSP header + button + script tag when configured), `TestOrdersModuleUnit` (direct create_order/verify_order_signature calls), `test_live_create_order_against_razorpay_sandbox` (`@pytest.mark.integration`, skipped unless `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` are in the env — the automated "really uses the test key" proof) | — |
| Fix — Razorpay `receipt` 40-char cap + gate-round truncated-verdict retry (post-#74, cold-reviewer #75) | C7,scripts | `web/routes_billing.py` `create_razorpay_order` — receipt shortened to `1x-{user_id[:8]}-{uuid:12}` (was `oneshot-{user_id:32}-{uuid:12}` = 53 chars, over Razorpay's 40-char cap → every LIVE `POST /orders` would 400; the mocked httpx boundary hid it), and `authz.ensure(MANAGE_BILLING)` moved BEFORE `session.commit()` so a non-owner attempt leaves no orphan user row. `services/payments/razorpay_orders.py` — outgoing payload `receipt[:40]` clamp (defense-in-depth for any future caller). `scripts/gate_round.py` — `_looks_truncated` + retry a verdict-only agent reply ONCE, then an honest NO-VERDICT "re-run" message (was a misleading bare FAIL that blocked #69/#75 on intermittently-truncated cold-reviewer output; can never mask a real FAIL, which carries findings) | tests/test_razorpay_checkout.py::TestOrderCreate (receipt ≤40 assertion on the request payload; a direct create_order clamp test) + tests/test_gate_round.py::TestLooksTruncated, ::TestTruncatedVerdictRetries (bare-verdict detection; retry recovers a substantive verdict; persistent truncation → NO-VERDICT not bare FAIL; substantive first reply is not retried) | — |
| NFR-01 | C4  | rules/* (import guard test)                | T-NFR-01 | concepts/how-it-works, engineering/testing |
| NFR-02 | Ops | Caddyfile, config                          | T-OPS-01 (manual) | engineering/security |
| NFR-03 | C9  | obs/ratelimit                              | T-NFR-03 | api/overview |
| NFR-04 | C2..C4 | runner (perf fixture 1M rows); bound amended to 660s on 4-vCPU class (founder 2026-07-20, D13 VPS measured 624s) | T-PERF-01 + D13 VPS re-validation (CHANGELOG 2026-07-19) | engineering/performance |
| NFR-05 | C9  | obs/logging, /healthz                      | T-OBS-01..02 | engineering/integration |
| NFR-06 | C9  | obs/errors                                 | T-OBS-03 | — |
| NFR-07 | C3  | pricing/coster reconcile                   | T-PRC-05 (property) | engineering/testing |
| NFR-08 | Ops | scripts/backup.sh + restore drill          | T-OPS-02 (manual, logged) | engineering/integration |
| NFR-09 | Ops | docker-compose, Caddyfile, RUNBOOK         | T-OPS-03 (manual) | engineering/integration |
| NFR-10 | C  | runner via BackgroundTasks, status API      | T-API-02 | api/overview |
| NFR-11 | all | config, models (UTC), report display       | T-NFR-11 | — |
| NFR-12 | C9  | obs/ratelimit (user-else-IP key)           | T-NFR-12 | api/overview |
| NFR-13 | C   | runner queue admission, status API          | T-API-06 | api/overview |
| NFR-14 | C1,C9 | api error handlers, obs/errors            | T-API-07 | api/overview |
| NFR-15 | Ops,C3 | pricing/data/prices.yaml, scripts/pricing_age.py, CI | T-NFR-15 | concepts/pricing-data |
| X-01..05 | — | reviewer checklist item                   | REV-X (PR template) | limits, engineering/security |

Coverage rule: `pytest --cov=src/tokenops_cost_auditor` ≥ 85% lines on services/*,
100% on pricing/coster and rules/findings estimators (money math).
Matrix maintenance: any new FR requires a row here in the same PR.
