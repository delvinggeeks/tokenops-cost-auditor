"""M-FLY-0 — the intelligence flywheel's data spine (docs/12 Stage 3).

Two modules, no more: frame (the training-row contract every learning rung
consumes) and cohort (the Honesty-Law ledger that says which rungs may even
exist yet). This package imports NO network, NO inference, and NEVER
touches services/rules or services/pricing — learned artifacts reach the
deterministic engine only as config values, so the T-NFR-01 guard and the
zero-token architecture survive every rung (PLAN-FLYWHEEL R-F4).
"""
